    let sopt=document.getElementById('select');
    let images=document.querySelectorAll('.gallery .product');
    sopt.onchange=()=>{
        var svalue=sopt.value;
        //console.log(svalue);
        if(svalue==="asc"){
            
        }
        images.forEach(filter=>{
            let name=filter.getAttribute('data-name');
        
        });
    };
  
   